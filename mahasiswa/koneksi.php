<?php
    $server = "localhost";
    $user = "root";
    $password = "";
    $db = "db_akademik";

    // Membuka koneksi ke database
    $koneksi = new mysqli($server, $user, $password, $db); //open connection
?>
